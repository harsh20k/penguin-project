# Feb 17 — Session log

- **Seed script (bcrypt):** `seed_local.py` failed with passlib/bcrypt 5.0 on Python 3.14 (`__about__` / 72-byte error). Switched password hashing to `sha256_crypt` in `seed_local.py`, `auth_router.py`, and `factor2_service.py`. Instrumentation used for debugging was removed.
- **Uvicorn:** Run from `backend/` as `uvicorn app.main:app --reload` (module:attribute, not `app.main.app`).
- **Frontend:** Implemented Penguin Auth client per plan: Vite + React + TypeScript, `api/client.ts`, `LoginForm`, `Factor2Question`, `Factor3Challenge`, `StepLayout`, App state (token, sessionId, currentStep). Fixed blank page: type-only imports for `verbatimModuleSyntax`, `#root { min-height: 100vh }`, ErrorBoundary in `main.tsx`.
- **Terraform Lambda IaC:** Added `backend/lambda_handler.py` (Mangum), `mangum` in requirements; `terraform/scripts/build_lambda.sh` → `dist/penguin-api.zip`; `terraform/` with provider, variables, Lambda (handler `lambda_handler.handler`, `DB_PATH=/tmp/local.db`), API Gateway HTTP API (ANY `/` and `ANY /{proxy+}`), IAM, outputs; `terraform/README.md` with build/apply and persistence caveat (SQLite ephemeral; EFS/DynamoDB later). *(Later replaced by seven per-route Lambdas without Mangum—see Feb 19.)*
- **Lambda 500 after deploy:** API Gateway returned `{"message":"Internal Server Error"}`. Debug showed `ModuleNotFoundError: No module named 'pydantic_core._pydantic_core'` — zip was built on macOS so native deps (pydantic_core) were wrong platform. **Fix:** In `terraform/scripts/build_lambda.sh`, install deps with `--platform manylinux2014_x86_64 --python-version 3.12 --only-binary=:all:` so the bundle uses Linux x86_64 wheels for Lambda. Added GET `/` in `app/main.py` so root URL returns a short JSON instead of 404.
